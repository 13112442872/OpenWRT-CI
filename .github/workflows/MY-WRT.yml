# SPDX-License-Identifier: MIT

# 自用固件编译：只编译 NN6000、京东云亚瑟、X86
name: 自用固件编译

on:
  # 自动编译：Auto-Clean 清理完成后自动运行
  workflow_run:
    workflows: ["Auto-Clean"]
    types:
      - completed

  # 手动编译：可以选择测试模式，也可以临时追加插件或内核配置
  workflow_dispatch:
    inputs:
      TEST:
        description: '测试模式：只输出配置文件，不编译固件'
        default: false
        required: false
        type: boolean
      PACKAGE:
        description: '临时追加配置：每行一条 CONFIG，例如 CONFIG_PACKAGE_luci-app-openclash=y'
        required: false
        type: string

# CI 权限
permissions: write-all

jobs:
  config:
    name: ${{matrix.CONFIG}}
    strategy:
      fail-fast: false
      matrix:
        include:
          # NN6000 v1/v2、京东云亚瑟，保留 Wi-Fi 驱动
          - CONFIG: MY-IPQ60XX-WIFI-YES
            SOURCE: VIKINGYFY/immortalwrt
            BRANCH: main

          # NN6000 v1/v2、京东云亚瑟，禁用 Wi-Fi 驱动
          - CONFIG: MY-IPQ60XX-WIFI-NO
            SOURCE: VIKINGYFY/immortalwrt
            BRANCH: main

          # X86 64 位固件
          - CONFIG: MY-X86
            SOURCE: VIKINGYFY/immortalwrt
            BRANCH: owrt

    # 继续复用原仓库公共编译核心，避免修改上游文件
    uses: ./.github/workflows/WRT-CORE.yml
    with:
      # 编译配置
      WRT_CONFIG: ${{matrix.CONFIG}}
      # 默认主题
      WRT_THEME: argon
      # 默认主机名
      WRT_NAME: OWRT
      # 默认 Wi-Fi 名称
      WRT_SSID: OWRT
      # 默认 Wi-Fi 密码
      WRT_WORD: 12345678
      # 默认管理地址
      WRT_IP: 192.168.10.1
      # 默认登录密码提示
      WRT_PW: 无
      # 源码仓库
      WRT_REPO: https://github.com/${{matrix.SOURCE}}.git
      # 源码分支
      WRT_BRANCH: ${{matrix.BRANCH}}
      # 源码名称
      WRT_SOURCE: ${{matrix.SOURCE}}
      # 固定插件、ShellCrash 依赖、临时追加配置
      WRT_PACKAGE: |
        CONFIG_PACKAGE_luci-app-aurora-config=n
        CONFIG_PACKAGE_luci-theme-aurora=n
        CONFIG_PACKAGE_luci-app-gecoosac=n
        CONFIG_PACKAGE_luci-app-tailscale=n
        CONFIG_PACKAGE_luci-app-homeproxy=n
        CONFIG_PACKAGE_luci-app-argon-config=y
        CONFIG_PACKAGE_luci-theme-argon=y
        CONFIG_PACKAGE_luci-app-adblock-fast=y
        CONFIG_PACKAGE_luci-app-frpc=y
        CONFIG_PACKAGE_luci-app-frps=y
        CONFIG_PACKAGE_luci-app-samba4=y
        CONFIG_PACKAGE_luci-app-rustdesk-server=y
        CONFIG_PACKAGE_bash=y
        CONFIG_PACKAGE_wget-ssl=y
        CONFIG_PACKAGE_ca-bundle=y
        CONFIG_PACKAGE_nftables-json=y
        CONFIG_PACKAGE_iptables-nft=y
        CONFIG_PACKAGE_ipset=y
        ${{inputs.PACKAGE}}
      # 是否只输出配置文件
      WRT_TEST: ${{inputs.TEST}}
